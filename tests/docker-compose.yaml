version: "3"

services:
  db:
    image: "postgres:11"
    container_name: "test_database"
    ports:
      - "54320:5432"
    environment:
      POSTGRES_DB: test_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres


